# README artwork

`matrix-homeserver-banner.png` was generated and then precisely edited with OpenAI image generation for the Matrix Homeserver README.

The prompt depicts a secure gateway, homeserver, durable database, browser client, and federated communications constellation without borrowing movie imagery or product logos. It requested the exact title “MATRIX HOMESERVER” and subtitle “Private chat. Federated by design.” A follow-up edit removed unwanted component labels while preserving the architecture. The final image was visually checked for accuracy and legible text before integration.
